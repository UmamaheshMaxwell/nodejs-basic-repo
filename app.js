console.log(`I have used nodemon to run the app`)

function getName (name) {
    return name
}

console.log(getName("Uma"))

const getUserName = function (name){
    return name
}

console.log(getUserName("Mahesh"))

const getFullName = (firstName, lastName) => {
    return firstName +  ' ' + lastName
}

console.log(getFullName("Umamaheswararao", "Meka"))

const array = ["Red", "Blue", "Yellow"]

array.forEach((item)=>{
    console.log(item)
})

const user  ={name: 'uma',city:'bangalore'}

console.log(user)